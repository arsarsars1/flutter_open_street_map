name: flutter_open_street_map
description: A flutter place search and location picker plugin that uses Open Street Map.
version: 0.0.3
homepage: https://github.com/MicroProgramer/flutter_open_street_map.git

environment:
  sdk: ">=2.17.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_map: ^2.2.0
  http: ^0.13.5
  latlong2: ^0.8.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1

flutter:
